# {{PROJECT_NAME}} — Project Memory

@AGENTS.md

> **Canonical rules live in `AGENTS.md`** (imported above — Claude Code inlines it at load, so the
> Laws, harness, and conventions are all in context). This shim adds only Claude-native conveniences
> other agents lack; durable project rules go in `AGENTS.md`, never here.

## Claude-native conveniences (Claude Code only)
- **Subagents** (own context, report back) for heavy reads — `preflight`, `tester`, `code-reviewer`, `codebase-audit`, `security-analyst`: from `.claude/agents/` (if vendored) or the caddis plugin.
- **Commands** — `/feature-plan`, `/tdd`, `/prd`, `/handoff`, `/implement`, `/ship`: from `.claude/commands/` or the plugin.
- **Skills** — reusable how-to, invoked by name / auto-triggered, from the caddis plugin.
- **Status line** — `caddis_statusline.py`, installed once per machine by `/caddis:statusline` (user scope, not per project).
